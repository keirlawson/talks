Various talks, built using reveal-md.

To invoke a talk simply install reveal-md via npm (`npm install -g reveal-md`) and then run it in the talk directory, passing the markdown file as the only parameter.
